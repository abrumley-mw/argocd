apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui-rollout-svc-active
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook-ui

---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui-rollout-svc-preview
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook-ui